-- import: fastn

-- fastn.package: hello

;; hello is the package name.
;; If you website is live then package name will be same as your site url
;; Notice: one line gap between between `import` and `fastn package`
